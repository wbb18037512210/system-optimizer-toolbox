feat: 融合 light-c 与 block-ads 到系统优化工具箱

LightC 2.15.0（Rust+Tauri 系统清理工具）：
- 新增 lightc_cleaners.py，把其文件型清理目标转换为原生 CLEAN_ITEMS：
  垃圾清理（传递优化/Defender LocalCopy/Support/D3D 着色器缓存/升级残留）、
  社交缓存（微信/QQ/企业微信/钉钉/飞书/Telegram，按安全子目录分级）、
  AI 模型缓存（HuggingFace/Ollama/LM Studio/ComfyUI，高危默认不勾选）。
- 主程序导入并 extend LIGHTC_CLEAN_ITEMS，CLEAN_ITEMS 扩至 200 项。
- 注册表/右键菜单/外壳图标/系统瘦身/旧驱动等需注册表或动态探测的能力
  超出文件清理模型，未移植（已在模块头注释说明）。

block-ads 1.3（Go 进程拦截工具）：
- 新增「进程拦截」面板 open_process_block：复刻其 folder/sign 黑名单白名单
  管理 + 后台监控（轮询运行进程并按目录/签名终止被拦截程序）。
- 规则持久化到 blockrules/（可由内置默认重建）；默认规则取自其 folder.txt/sign.txt。
- 后台监控经 PowerShell CIM 枚举进程、按 VersionInfo.CompanyName 匹配签名，
  UTF-8 容错解码；线程安全日志；停止时分段睡眠可及时退出。

说明：Python 版为“监控并阻止运行”，非驱动级“启动即拦截”（已与用户确认）。
